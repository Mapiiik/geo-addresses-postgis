[pytest]
# Repo root on the path so `api` and `tests` import the same way they do in
# the container, without an installable package.
pythonpath = .
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
